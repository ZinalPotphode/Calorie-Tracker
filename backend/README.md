# Calorie Tracker Backend

API server for the Calorie Tracker prototype. Uses Express and MongoDB (Mongoose).

Scripts:

- `npm install` to install dependencies
- `npm run seed` to create seed data (demo user + foods)
- `npm run dev` to start server with nodemon

